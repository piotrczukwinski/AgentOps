{
  "version": 1,
  "roadmap_id": "local-shell-smoke",
  "repo": {
    "id": "agentops-smoke",
    "path": "./examples/repos/local-shell-smoke",
    "base_branch": "HEAD"
  },
  "defaults": {
    "executor": "shell",
    "execution_mode": "worktree_branch",
    "branch_prefix": "agentops",
    "max_attempts": 1,
    "timeout_seconds": 120,
    "auto_commit": false,
    "auto_push": false
  },
  "policies": {
    "forbidden_branches": ["main", "master", "audit/**"],
    "forbidden_globs": [".env", ".env.*", "data/**", "evidence/**", "exports/**", "migrations/**", "alembic/**"]
  },
  "tasks": [
    {
      "id": "LOCAL-SHELL-SMOKE-001",
      "kind": "demo",
      "risk": 1,
      "executor": "shell",
      "executor_command": "python3 -c \"from pathlib import Path; Path('agentops-smoke-output.txt').write_text('agentops local shell smoke ok\\n', encoding='utf-8')\"",
      "prompt": "../prompts/local-shell-smoke.md",
      "allowed_files": ["agentops-smoke-output.txt"],
      "validations": [
        "python3 -c \"from pathlib import Path; assert Path('agentops-smoke-output.txt').read_text(encoding='utf-8') == 'agentops local shell smoke ok\\n'\"",
        "git diff --check"
      ],
      "review": {"codex": "never"}
    }
  ]
}
